import React from "react";

function WeekSearch() {
  return (
    <div className="container">
      <button>Click here to view other weeks</button>
    </div>
  );
}

export default WeekSearch;

// have something that will show the weeks, you select that and it will change the display to show the chart for that week
